[project]
name = "ptx-pretraining"
version = "0.1.0"
description = "Pretraining Transformer encoder on PTX kernels"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "black>=26.1.0",
    "pytest>=9.0.2",
    "ruff>=0.14.14",
]
